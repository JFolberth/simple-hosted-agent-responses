name: Deploy

on:
  workflow_call:
    inputs:
      image_tag:
        description: Image tag produced by the build job
        required: true
        type: string
      acr_endpoint:
        description: ACR endpoint (e.g. myregistry.azurecr.io)
        required: true
        type: string
      image_name:
        description: Container image name
        required: true
        type: string
      agent_name:
        description: Foundry hosted agent name
        required: true
        type: string
      environment_name:
        description: Environment name (used in resource naming)
        required: true
        type: string
      location:
        description: Azure region for the resource group
        required: true
        type: string
      ai_deployments_location:
        description: Azure region for AI model deployments
        required: true
        type: string
    secrets:
      AZURE_CLIENT_ID:
        required: true
      AZURE_TENANT_ID:
        required: true
      AZURE_SUBSCRIPTION_ID:
        required: true

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  # ── Push image to ACR ───────────────────────────────────────────────────────
  push-image:
    name: Push image to ACR
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Download image artifact
        uses: actions/download-artifact@v4
        with:
          name: agent-image

      - name: Load Docker image
        run: docker load < agent-image.tar.gz

      - name: Azure login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Authenticate to ACR
        run: |
          ACR_ENDPOINT="${{ inputs.acr_endpoint }}"
          az acr login --name "${ACR_ENDPOINT%.azurecr.io}"

      - name: Tag and push image
        run: |
          FULL_IMAGE="${{ inputs.acr_endpoint }}/${{ inputs.image_name }}:${{ inputs.image_tag }}"
          docker tag "${{ inputs.image_name }}:${{ inputs.image_tag }}" "${FULL_IMAGE}"
          docker push "${FULL_IMAGE}"

  # ── Deploy Bicep IaC + update agent ────────────────────────────────────────
  deploy-bicep:
    name: Deploy (Bicep)
    needs: push-image
    uses: ./.github/workflows/deploy-bicep.yml
    with:
      image: "${{ inputs.acr_endpoint }}/${{ inputs.image_name }}:${{ inputs.image_tag }}"
      agent_name: ${{ inputs.agent_name }}
      environment_name: ${{ inputs.environment_name }}
      location: ${{ inputs.location }}
      ai_deployments_location: ${{ inputs.ai_deployments_location }}
    secrets: inherit

  # ── Deploy Terraform IaC + update agent ────────────────────────────────────
  deploy-terraform:
    name: Deploy (Terraform)
    needs: push-image
    uses: ./.github/workflows/deploy-terraform.yml
    with:
      image: "${{ inputs.acr_endpoint }}/${{ inputs.image_name }}:${{ inputs.image_tag }}"
      agent_name: ${{ inputs.agent_name }}
      environment_name: ${{ inputs.environment_name }}
      location: ${{ inputs.location }}
      ai_deployments_location: ${{ inputs.ai_deployments_location }}
    secrets: inherit
